services:
  bot:
    build: .
    image: tochkaico/discord-meowser-bot:latest
    container_name: discord-meowser-bot
    restart: always
    env_file:
      - .env
    environment:
      - BOT_TOKEN=${BOT_TOKEN}